spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/PracticeJavaSpringReactJS
    driver-class-name: org.postgresql.Driver
    username: dbs_user
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

